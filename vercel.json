{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "tsc && vite build",
        "outputDirectory": "dist"
      }
    }
  ]
} 